.navbar.navbar-inverse.navbar-fixed-top{role: 'navigation'}
  .container
    .navbar-header
      %button.navbar-toggle{'data-target' => '.navbar-collapse', 'data-toggle' => 'collapse', :type => 'button'}
        %span.sr-only Toggle navigation
        %span.icon-bar
        %span.icon-bar
        %span.icon-bar
      %span.navbar-brand Airbrake Explorer
    .collapse.navbar-collapse
      %ul.nav.navbar-nav
        %li.dropdown
          %a.dropdown-toggle{href: '#', 'data-toggle' => 'dropdown'}
            Projects
            %b.caret
          %ul.dropdown-menu
            - projects.each do |project|
              %li
                %a{href: "/?project_id=#{project.id}"} #{project.name}
        %li
          %a{href: "/?project_id=#{current_project.id}"} #{current_project.name}
